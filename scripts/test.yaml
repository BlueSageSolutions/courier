- script: test
  sources:
  setup:
    - executable: bash
      directory: /Users/jploughman/projects/deploy-scripts/
      command: setClient
      arguments:
        - name: client
          style: plain
          value: mfm
  main:
    # - description: check the alarms
    #   executable: bash
    #   directory: /Users/jploughman/projects/deploy-scripts/
    #   environment: 
    #     - config-common
    #     - config-common-db
    #     - config-mfm
    #   command: checkAlarms
